---
description: 本仓库开发必须遵循 Superpowers 技能流程（计划、TDD、验证、评审）
alwaysApply: true
---

# Superpowers 开发流程（mdtest）

在本项目中实现功能、修 bug、重构行为时，**必须**按 Superpowers 技能顺序执行，不得跳过。

## 标准流程

1. **brainstorming** — 新功能、改行为、架构/UX 决策前：澄清意图、约束与方案，用户确认后再写代码。
2. **writing-plans** — 多步骤或跨文件任务：先写实现计划，保存到 `docs/superpowers/plans/YYYY-MM-DD-<feature>.md`（无目录则创建）。
3. **using-git-worktrees**（可选）— 大功能或长计划：在独立 worktree 中开发，避免污染主工作区。
4. **test-driven-development** — 功能与 bugfix：先写失败测试 → 实现 → 测试通过（项目暂无测试栈时，在计划中注明验证命令，如 `pnpm run build` / `pnpm check`）。
5. **subagent-driven-development** 或 **executing-plans** — 按计划逐步执行，每步可检查点。
6. **systematic-debugging** — 任何 bug、测试失败、异常行为：先根因分析，再提修复，禁止未验证就改。
7. **verification-before-completion** — 声称「完成/修好/通过」前：必须运行验证命令并依据输出说明结果。
8. **requesting-code-review** — 较大功能完成后：对照计划做自检或 code-reviewer 子代理评审。
9. **finishing-a-development-branch** — 收尾时：合并 / PR / 清理等选项交给用户选择。

## 执行要求

- 适用技能时**先读取对应 SKILL.md 再行动**（Cursor 环境用 Read 工具读插件内 skill 路径）。
- 使用技能时在回复中简要说明（例如：「使用 writing-plans 编写实现计划」）。
- 计划任务用 TodoWrite 跟踪；有 checklist 的技能逐项勾选。
- 用户明确要求「直接改、不要计划」时，可缩小范围，但仍需 **verification-before-completion**。

## 与本项目相关的默认验证

```bash
pnpm run build
pnpm run check   # 若已配置
```

## UI / 设计（与 Superpowers 并行）

- 改界面、定视觉、UI 评审：读取 `.cursor/skills/pomelosuki/SKILL.md` 与 `references/design-principles.md`，或委派 **pomelosuki** 子代理（`.cursor/agents/pomelosuki.md`）。
- UI 任务仍须走 brainstorming → writing-plans（可合并为短计划），实现后 `pnpm run build`。
- 不重复安装 WorkBuddy 的 ui-ux-pro-max / frontend-design / general-skills；等价关系见 `docs/pomelosuki/cursor-equivalence.md`。
- 全员分工见 `team-roles.mdc`（主代理 / Superpowers / 柚爱）。

## 不必套用的场景

- 纯问答、文档说明、列目录、读 skill 列表等不涉及改代码的请求。
- 单行 typo / 用户明确「跳过计划快改」的极小改动（仍需 build 验证若改了代码）。
