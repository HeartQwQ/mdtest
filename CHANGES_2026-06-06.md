# 修改记录 — 2026-06-06

本文档详细记录了今天对项目所做的全部修改，涵盖 iOS 刷新风暴修复、安卓应用包目录改造、前端文件浏览模式切换等。

---

## 一、iOS 刷新风暴修复

### 问题描述
iOS 设备通过 `ideviceinfo` 轮询获取设备信息时，`details` 字段可能包含每次轮询动态变化的内容（如电池电量、运行时状态等），导致 `snapshot()` 每次生成的字符串不同，即使设备列表实质未变，也会触发无意义的 `broadcast`，前端收到事件后调用 `setDevices` 引发 Svelte 响应式重渲染，造成"刷新风暴"。

### 修复方案（三层防护）

#### 1. 后端：`snapshot()` 排除不稳定字段
**文件**：[device-monitor.ts](src/main/devices/device-monitor.ts)

修改前：
```typescript
function snapshot(devices: DeviceInfo[]): string {
  return JSON.stringify(devices)
}
```

修改后：
```typescript
function snapshot(devices: DeviceInfo[]): string {
  // 只比对稳定字段：id + status + name。
  // 排除 details —— ideviceinfo 每次轮询可能返回微小差异的字段，
  // 导致 snapshot 每次不同从而触发无意义的 broadcast。
  return JSON.stringify(
    devices.map((d) => ({ id: d.id, status: d.status, name: d.name }))
  )
}
```

**效果**：`snapshot()` 只提取 `id`、`status`、`name` 三个稳定字段，忽略 `details` 字段的动态变化。只有在设备列表真正发生变化（设备增减或状态变化）时才会触发 `broadcast`。

#### 2. 缓存：`isSameSnapshot` 智能刷新
**文件**：[dir-cache.ts](src/main/files/dir-cache.ts)

新增 `isSameSnapshot` 方法，用于判断目录内容是否真正发生变化：
```typescript
/** 判断两个条目列表是否相同（智能刷新） */
isSameSnapshot(key: CacheKey, entries: FileEntry[]): boolean {
  const cached = this.map.get(key)
  if (!cached) return false
  return cached.snapshot === makeSnapshot(entries)
}
```

配合 `makeSnapshot` 函数生成条目快照（只取 `name` + `isDirectory` + `size`）：
```typescript
function makeSnapshot(entries: FileEntry[]): string {
  return entries.map((e) => `${e.name}|${e.isDirectory ? 'd' : 'f'}|${e.size ?? 0}`).join('\n')
}
```

**效果**：后台刷新陈旧缓存时，如果内容未变（快照一致），则跳过缓存更新，避免不必要的 UI 刷新。

#### 3. 前端：`setDevices` 实质变化检测
**文件**：[DeviceManagementView.svelte](src/renderer/src/lib/views/DeviceManagementView.svelte)

新增 `hasMaterialChange` 函数，在 `setDevices` 中检测设备列表是否有实质变化（仅比对 `id` 和 `status`）：

```typescript
function hasMaterialChange(prev: DeviceInfo[], next: DeviceInfo[]): boolean {
  if (prev.length !== next.length) return true
  for (let i = 0; i < prev.length; i++) {
    if (prev[i].id !== next[i].id || prev[i].status !== next[i].status) return true
  }
  return false
}

function setDevices(platform: DevicePlatform, list: DeviceInfo[]): void {
  const prevList = devicesByPlatform[platform] ?? []
  // 设备列表无实质变化时跳过更新，避免 iOS 轮询每次都触发 Svelte 响应式重渲染
  if (!hasMaterialChange(prevList, list)) return
  // ... 更新逻辑
}
```

**效果**：即使收到 `devices:changed` 事件，如果设备列表实质未变（id 和 status 不变），也不会触发前端重渲染。

---

## 二、`execAdbBinary` stderr 分离

### 问题描述
`execAdbBinary` 将 `stdout` 和 `stderr` 混在一起返回，当 adb shell 子命令产生 stderr 错误信息（如 `/bin/sh: pm: inaccessible or not found`）时，错误信息会被拼接到路径中，导致后续路径解析出错，例如变成 `/storage/emulated/0/Android/data//bin/sh: pm: inaccessible or not found/`。

### 修复方案
**文件**：[adb-path.ts](src/main/devices/adb-path.ts)

修改后的 `execAdbBinary`：
```typescript
async function execAdbBinary(adbPath: string, args: string[]): Promise<string> {
  const dir = dirname(adbPath)
  const { stdout, stderr } = await execFileAsync(adbPath, args, {
    timeout: 20000,
    cwd: dir,
    windowsHide: true,
    env: adbEnv(dir),
    maxBuffer: 4 * 1024 * 1024
  })

  // adb daemon 提示（以 * 开头）属于正常信息，保留合并；
  // 但 shell 子命令的 stderr 是真正的错误输出（如 "/bin/sh: pm: not found"），
  // 不应混入结果，否则会污染后续解析。
  if (stderr) {
    const isDaemonNotice = stderr.split(/\r?\n/).every(
      (line) => !line.trim() || line.trim().startsWith('*')
    )
    if (isDaemonNotice) {
      // daemon 提示属于正常信息，合并到输出
      return [stdout, stderr].filter(Boolean).join('\n')
    }
    // shell 命令的 stderr 是错误，仅日志化，不混入返回值
    logWarn('adb', `stderr (非 daemon 提示，已忽略): ${stderr.slice(0, 200)}`)
  }

  return stdout
}
```

**关键逻辑**：
- adb daemon 提示（以 `*` 开头的行，如 `* daemon not running; starting now at tcp:5037`）属于正常信息，保留合并到返回结果中。
- shell 子命令的 stderr（如权限错误、命令未找到等）是真正的错误输出，仅记录日志，不混入返回值。

---

## 三、安卓应用包目录改造

### 需求背景
移动端（安卓/iOS/鸿蒙）添加"已安装的应用包目录"功能，默认显示应用包目录，可切换到根目录浏览手机文件。用户明确要求安卓应用包目录直接显示 `/storage/emulated/0/Android/data` 目录（该目录下每个子目录对应一个应用包），不需要定位到具体的 `/${pkg}`。

### 修改 1：精简 `ANDROID_CANDIDATES`
**文件**：[mobile-data-path.ts](src/main/files/mobile-data-path.ts)

移除了 `/files` 子目录的候选路径，因为安卓应用包目录只需展示到 `/storage/emulated/0/Android/data` 级别：

修改前：
```typescript
const ANDROID_CANDIDATES = (pkg: string) => [
  `/storage/emulated/0/Android/data/${pkg}`,
  `/storage/emulated/0/Android/data/${pkg}/files`,
  `/sdcard/Android/data/${pkg}`,
  `/sdcard/Android/data/${pkg}/files`
]
```

修改后：
```typescript
const ANDROID_CANDIDATES = (pkg: string) => [
  `/storage/emulated/0/Android/data/${pkg}`,
  `/sdcard/Android/data/${pkg}`
]
```

### 修改 2：`resolvePackageDataRoot` 安卓模式直接返回公共父目录
**文件**：[mobile-data-path.ts](src/main/files/mobile-data-path.ts)

安卓模式下不再拼接 `/${applicationId}`，直接返回 `/storage/emulated/0/Android/data` 作为应用包列表的根目录：

修改后：
```typescript
export async function resolvePackageDataRoot(
  platform: MobilePlatform,
  deviceId: string,
  applicationId: string
): Promise<string> {
  // 安卓：直接返回 /storage/emulated/0/Android/data 作为应用包列表的根目录
  // 该目录下的每个子目录就是一个应用包，无需定位到具体的 /${applicationId}
  if (platform === 'android') {
    return '/storage/emulated/0/Android/data'
  }

  // 鸿蒙：通过候选路径探测
  const candidates = HARMONY_CANDIDATES(applicationId)
  for (const p of candidates) {
    if (await remotePathExists(platform, deviceId, p)) return p
  }
  return `/data/app/el2/100/database/${applicationId}`
}
```

### 修改 3：`loadAndCacheAppDir` 安卓应用包模式
**文件**：[device-fs.ts](src/main/files/device-fs.ts)

安卓应用包模式下，直接设置 `root='/storage/emulated/0/Android/data'`，不传 `packageId` 给 `listMobileDir`，这样就能列出该目录下的所有应用包子目录：

```typescript
} else if (platform === 'android') {
  // 安卓应用包模式：直接列出 /storage/emulated/0/Android/data 下的所有应用包目录
  // 不需要传 packageId，因为每个子目录就是一个应用包
  root = '/storage/emulated/0/Android/data'
  const res = await listMobileDir(
    platform as MobilePlatform,
    deviceId,
    '',
    relativePath,
    root
  )
  entries = res.entries
  hint = '每个子目录对应一个已安装的应用包，点击可进入该应用的数据目录。'
}
```

### 修改 4：`refreshAppDir` 同步修改
**文件**：[device-fs.ts](src/main/files/device-fs.ts)

刷新逻辑与 `loadAndCacheAppDir` 保持一致，安卓应用包模式下同样直接设置 `root='/storage/emulated/0/Android/data'`。

### 修改 5：新增 `activePackageId` 状态
**文件**：[device-fs.ts](src/main/files/device-fs.ts)

在后端 `device-fs.ts` 中新增了对 `activePackageId` 的追踪。当用户在安卓应用包模式下进入某个具体应用包子目录时，该子目录名自动成为新的 `activePackageId`，后续对该应用包的文件操作（读取、写入、删除、创建文件夹等）都基于这个新的 `packageId`。

---

## 四、前端文件浏览模式切换（FileExplorer.svelte）

### 修改概述
**文件**：[FileExplorer.svelte](src/renderer/src/lib/components/FileExplorer.svelte)

前端 `FileExplorer` 组件新增了"应用包/根目录"切换功能，支持安卓应用包模式下动态追踪 `activePackageId`。

### 修改 1：扩展 `mobilePlatform` 类型
```typescript
mobilePlatform?: DeviceFsPlatform  // 含 ios
```

### 修改 2：新增 `fileMode` prop 和状态
```typescript
/** 移动端文件视图模式：'app'=应用包目录（默认），'root'=设备根目录 */
fileMode?: 'app' | 'root'

/** 当前实际文件浏览模式（仅移动端有效） */
let activeFileMode = $state<'app' | 'root'>('app')

/** 当前活跃的应用包ID（安卓应用包模式下动态追踪） */
let activePackageId = $state<string | undefined>(packageId)
```

### 修改 3：`effectiveMode` 派生状态
```typescript
const effectiveMode = $derived.by(() => {
  if (mode === 'local') return 'local'
  // 移动端：根据activeFileMode决定实际模式
  if (mode === 'mobile' || mode === 'device') {
    if (activeFileMode === 'root') return 'device'
    return 'mobile'
  }
  return mode
})
```

当 `activeFileMode` 为 `'root'` 时，`effectiveMode` 变为 `'device'`（设备根目录浏览模式）；当 `activeFileMode` 为 `'app'` 时，`effectiveMode` 为 `'mobile'`（应用包目录浏览模式）。

### 修改 4：`contextKey` 和 `getCacheOpts` 使用新状态
`contextKey` 和 `getCacheOpts` 均使用了 `activePackageId` 和 `activeFileMode`，确保模式切换或应用包切换时触发重新加载。

### 修改 5：`loadDir` 移动端分支——安卓允许空 `activePackageId`
```typescript
// 安卓应用包模式下不需要 activePackageId 即可列出 /storage/emulated/0/Android/data 目录
// 当用户进入某个具体应用包子目录后，该子目录名会成为新的 activePackageId
// 非安卓平台（鸿蒙）必须提供 packageId，安卓允许空字符串
const effectivePkgId = mobilePlatform === 'android'
  ? (activePackageId ?? '')
  : (activePackageId ?? '')
if (effectivePkgId !== '' || mobilePlatform === 'android') {
  const res = await filesApi.listApp(mobilePlatform, deviceId, effectivePkgId, path)
  // ...
}
```

### 修改 6：`openEntry` 安卓应用包模式下更新 `activePackageId`
```typescript
if (effectiveMode === 'mobile' && mobilePlatform === 'android' && ent.isDirectory) {
  activePackageId = ent.name
}
```

当用户在安卓应用包模式下点击一个子目录（即某个应用包目录）时，将该目录名设为新的 `activePackageId`，后续的文件操作将基于这个应用包。

### 修改 7：所有 CRUD 操作使用 `activePackageId`
文件创建、删除、文件夹创建、上传等操作均使用了 `activePackageId`，确保在正确的应用包路径下操作。

### 修改 8：切换按钮 disabled 条件
```typescript
disabled={mobilePlatform !== 'android' && !activePackageId}
```

安卓平台不需要 `activePackageId` 就能切换（因为默认列出 `/storage/emulated/0/Android/data`），非安卓平台必须有 `activePackageId` 才能启用"应用包"按钮。

### 修改 9：应用包/根目录切换按钮 UI
```svelte
<div class="flex items-center gap-0.5 rounded-md bg-muted/50 p-0.5">
  <button
    type="button"
    class={cn(
      'flex items-center gap-1 rounded-sm px-1.5 py-0.5 text-[11px] font-medium transition-colors',
      activeFileMode === 'app'
        ? 'bg-background text-foreground shadow-sm'
        : 'text-muted-foreground hover:text-foreground'
    )}
    onclick={() => switchFileMode('app')}
    disabled={mobilePlatform !== 'android' && !activePackageId}
    title="应用包目录"
  >
    <Package class="size-3" strokeWidth={1.75} />
    <span>应用包</span>
  </button>
  <button
    type="button"
    class={cn(
      'flex items-center gap-1 rounded-sm px-1.5 py-0.5 text-[11px] font-medium transition-colors',
      activeFileMode === 'root'
        ? 'bg-background text-foreground shadow-sm'
        : 'text-muted-foreground hover:text-foreground'
    )}
    onclick={() => switchFileMode('root')}
    title="设备根目录"
  >
    <HardDrive class="size-3" strokeWidth={1.75} />
    <span>根目录</span>
  </button>
</div>
```

### 修改 10：`switchFileMode` 函数和 `$effect` 同步
```typescript
function switchFileMode(newMode: 'app' | 'root') {
  if (activeFileMode === newMode) return
  activeFileMode = newMode
}

$effect(() => {
  // 同步外部fileModeProp变化
  if (fileModeProp) {
    activeFileMode = fileModeProp
  }
})
```

---

## 五、DeviceManagementView.svelte 应用包选择 UI

### 修改概述
**文件**：[DeviceManagementView.svelte](src/renderer/src/lib/views/DeviceManagementView.svelte)

设备管理主视图新增了应用包选择功能，包括搜索框、下拉菜单和刷新按钮。

### 修改 1：添加 Package 图标和 Input/Button 组件导入
```typescript
import { Package } from '@lucide/svelte'
import { Button } from '$lib/components/ui/button'
import { Input } from '$lib/components/ui/input'
```

### 修改 2：移动端 FileExplorer 传入 `packageId`
```svelte
<FileExplorer
  mode="mobile"
  root=""
  mobilePlatform={selectedDevice.platform as DeviceFsPlatform}
  deviceId={selectedDevice.id}
  packageId={selectedPackageId ?? undefined}
/>
```

### 修改 3：应用包选择 UI
包含搜索过滤框、下拉菜单（显示已安装应用包列表）和刷新按钮。

### 修改 4：`setDevices` 实质变化检测
（详见本文档"一、iOS 刷新风暴修复"中的"前端：setDevices 实质变化检测"部分）

---

## 六、缓存智能刷新机制（dir-cache.ts）

### 修改概述
**文件**：[dir-cache.ts](src/main/files/dir-cache.ts)

新增了 `isSameSnapshot` 方法和 `makeSnapshot` 辅助函数，实现缓存智能刷新。

**核心逻辑**：
- `set` 方法写入缓存时，先计算新条目的快照（`makeSnapshot`），与已有缓存快照比对。
- 如果快照一致，仅更新时间戳，不替换缓存内容，避免触发不必要的 UI 刷新。
- 如果快照不同或为新增条目，正常写入缓存并更新 LRU 顺序。

```typescript
set(key: CacheKey, data: { entries: FileEntry[]; root?: string; hint?: string }): void {
  const snapshot = makeSnapshot(data.entries)
  const existing = this.map.get(key)
  // 智能刷新：如果快照一致，仅更新时间戳
  if (existing && existing.snapshot === snapshot) {
    existing.ts = Date.now()
    this.touch(key)
    return
  }
  // 新增或内容变化
  this.map.set(key, { ... })
  this.touch(key)
  // 淘汰最旧的
  while (this.map.size > MAX_MEMORY_ENTRIES) {
    const oldest = this.order.shift()
    if (oldest) this.map.delete(oldest)
  }
}
```

---

## 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/main/devices/device-monitor.ts` | 修改 | `snapshot()` 排除 `details` 字段，修复 iOS 刷新风暴 |
| `src/main/devices/adb-path.ts` | 修改 | `execAdbBinary` 分离 stderr，修复路径解析错误 |
| `src/main/files/mobile-data-path.ts` | 修改 | 精简 `ANDROID_CANDIDATES`，`resolvePackageDataRoot` 安卓模式返回公共父目录 |
| `src/main/files/device-fs.ts` | 修改 | `loadAndCacheAppDir` 和 `refreshAppDir` 安卓应用包模式改造，新增 `activePackageId` 追踪 |
| `src/main/files/dir-cache.ts` | 修改 | 新增 `isSameSnapshot` 智能刷新，`set` 方法快照比对 |
| `src/renderer/src/lib/components/FileExplorer.svelte` | 修改 | 新增 `fileMode`/`activeFileMode`/`activePackageId` 状态，应用包/根目录切换按钮，安卓允许空 `activePackageId`，`openEntry` 动态更新 `activePackageId` |
| `src/renderer/src/lib/views/DeviceManagementView.svelte` | 修改 | `setDevices` 实质变化检测，应用包选择 UI，FileExplorer 传入 `packageId` |
